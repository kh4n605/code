<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
$(document).ready(function() {
  // Clild Sectors and selectors we are going to hide need to separate with comma
  $('.child_selector').closest('.closest_parent_div').find('selectors needs to hide').hide();
  $('.child_selector').closest('.closest_parent_div').find('selectors needs to hide').show();
});	